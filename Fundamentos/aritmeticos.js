//Operador aritmético é binário 

const [a, b, c, d] = [3, 5, 1, 15]

const soma = a + b + c + d
const subtracao = d - b 
const multiplicacao = a * b
const divisao = d / a
const modulo = a % 2 //Divide a por 2 , e o resto da conta é o resultado

console.log(soma, subtracao, multiplicacao, divisao, modulo)
